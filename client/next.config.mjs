/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            "localhost",
            "xsgames.com",
            "files.edstore.dev",
            "raw.githubusercontent.com",
            "files.edgestore.dev",
            "images.remotePatterns",
        ],
    },
};

export default nextConfig;
